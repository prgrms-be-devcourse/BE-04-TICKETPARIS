<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.programmers.ticketparis.mapper.ReservationMapper">
    <insert id="create" useGeneratedKeys="true" keyProperty="reservationId">
        insert into reservation (status, customer_id, schedule_id)
        values (#{reservationStatus}, #{customerId}, #{scheduleId})
    </insert>

    <update id="cancel">
        update reservation
        set status=#{reservationStatus}
        where reservation_id = #{reservationId}
    </update>

    <select id="existById" resultType="boolean">
        select exists(select 1 from reservation where reservation_id = #{reservationId})
    </select>
</mapper>
